<!DOCTYPE html>
<html>

<head>
  <title>Homes</title>
  <style>
    button {
      padding: 8px 16px;
      background-color: #4CAF50;
      color: white;
      border: none;
      cursor: pointer;
    }

    button:hover {
      background-color: #45a049;
    }

    .card {
      /* Add shadows to create the "card" effect */
      box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
      transition: 0.3s;
      border-radius: 5px;

    }

    /* On mouse-over, add a deeper shadow */
    .card:hover {
      box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
    }

    /* Add some padding inside the card container */
    .container {
      padding: 2px 16px;
    }

    /* Add flex for card container */
    .card-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
    }

    header {
      background-color: #f1f1f1;
      text-align: center;
      font-size: 25px;
    }
  </style>
</head>

<body>
  <header>
    <h1>Homes</h1>
  </header>
  <div class="card-container">
    <% for (let i=0; i < data.length; i++) { %>
      <div class="card" onclick="handleButtonClick('<%= data[i].uuid %>')">
        <div class="container">
          <h4><b>
              <%= data[i].address %>
            </b>
          </h4>
          <p>Lorem ipsum</p>
        </div>
      </div>
      <% } %>
  </div>

  <script>
    function handleButtonClick(uuid) {
      window.location.href = '/devices?gateway=' + uuid;
    }
  </script>
</body>

</html>